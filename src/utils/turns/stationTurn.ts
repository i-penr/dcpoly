/* import type { Game } from "../../db/tables/Game";
import type { Player } from "../../db/tables/Player";
import type DiscordResponse from "../../models/classes/DiscordResponse";

export async function stationTurn(square: number, game: Game, responseBuilder: DiscordResponse, player: Player) {
    const owner = "";
} */
